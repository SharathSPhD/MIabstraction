# Gates — what "done" means here

The objective this file exists to protect: **anybody writes a high-level Loom program
and gets an LLM tuned to their objectives and data — not RAG, not fine-tuning, not
agent-building, but the language model itself made programmatically — and the app
lets them work with the model they built.**

A gate closes only on evidence a reader can re-derive: a committed artifact, a
command whose output is quoted, or a test that fails if the claim stops being true.
"Implemented" is not a closure. "Looks right" is not a closure. A gate that was
closed and later found untrue is re-opened with the reason recorded, because the
history of a wrong closure is more useful than a clean table.

| Gate | Promise | Evidence required | Status |
|---|---|---|---|
| **A** | Refusal is not compiled into weights | `grep -r REFUSAL_DEMOS\|_refusal_margin src/` empty; catalogue has no PROHIBITION/GUARDRAIL; a built Counsel answers a legal question | **CLOSED 2026-08-02** — 215 lines deleted; Counsel passes and answers "what does a motion to dismiss test?" through the deployed app |
| **C** | The app talks to the model you built | Deployed `/api/gpu/artifacts` lists the live library; a chat request through the public URL returns the model's words; a failed turn keeps the question | **CLOSED 2026-08-02** — 24 artifacts (was 1, a build-time snapshot); Counsel answered in 4s via loom-studio-tan.vercel.app |
| **B1** | A verified circuit is linked into a real build, not an experiment | A build report names a linked unit, its firing condition, its solved gain and the host's measured cost | **CLOSED 2026-08-02, re-measured same day** — first closure reported the gate on the traffic its gain was fitted to, which passes by construction (Phase B audit, finding 2). Traffic is now split: gain fitted on 8 sequences, everything reported measured on the other 8. Claim survives — skill 0.060 → 0.349, host *gains* 0.41 nats (the unit writes only where the context repeats, and on those positions it predicts better than the host did, so the host's average loss falls rather than rises), fires on 70%; 7 tests, two of which fail if the split is ever removed. **Standing limitation, raised by the Phase D audit**: no *real* build report carries this record. The circuit's verified envelope tops out far below the 16k–32k vocabularies real programs compile to, so `exec_scratch` calls the linker and the linker honestly refuses with its envelope numbers. The path is live and measured; what it has not yet done is link inside a shipped model |
| **B2** | Policy is enforced by an intermediary, not by weights | A request outside the declared subject is handled without the model having been trained to refuse; in-subject answers are byte-identical to the unlinked model | **SPLIT 2026-08-02.** **B2a CLOSED** — the architecture holds: policy is a gate in front of an unmodified model, so in-subject behaviour is preserved exactly, and `exec_scratch` now carries `rep.policy` so scratch builds can enforce it too. **B2b OPEN** — the *estimator* inside the gate does not generalise. Word overlap at the shipped 0.34 floor gets 31 of 76 requests wrong across the eight domains with contrast material, and on grammar it gates every in-subject question. The floor is now calibrated per build and the gate disables itself where its material cannot separate the classes (`results/policy_gate_resolution.json`). Scope detection that works on every domain needs activation-level recognition, not a better constant |
| **D1** | Every measured field the compiler produces is visible in the app | Layer walk shows `realized`, held-out PPL, `variety_after`, controls, search space | **CLOSED 2026-08-02** — the layer walk carries measured-vs-planned, held-out perplexity, `variety_after`, the search space, the linked unit, the policy section, and the scratch build's tokenizer, tokens and weight location. Extended the same day with the three fields that make a perplexity interpretable: passes over the training set, what the held-out text was held out *from*, and the loss curve's endpoints — a report without them hid a 365-epoch run behind a single number |
| **D2** | Scratch builds appear in the showcase | A from-scratch artifact is listed, opens, and can be chatted with from the browser | **CLOSED 2026-08-02** — `scripts/promote_artifacts.py` promotes 13 artifacts into `results/`. Verified live rather than by inspection: `scribe-flagship` (`scratch(medium)`) is one of 25 artifacts the deployed app lists, and answering "The old house stood at the end of the" through loom-studio-tan.vercel.app returned the model's own prose over Vercel → Cloudflare → tunnel → GB10 |
| **D3** | `/use` is a place to work, not a debug console | Preselect from a build, compare against base, persistent history, export | **CLOSED 2026-08-02** — `/use?artifact=` preselects and says so when the name is stale; history persists per artifact across reloads; the transcript exports as markdown carrying the controls and doses that produced it; a comparison turn puts the same question to the untouched base model beside yours, and on a from-scratch build says there is no base whose credit could be subtracted. When the policy gate answers, the page says the model was not consulted rather than letting it read as the model declining |
| **D4** | Steering capacity is measured widely enough to drive compiler decisions | ≥5 model families in the ledger, or the skip heuristic withdrawn | **CLOSED 2026-08-02** — `scripts/capacity_across_families.py` over Llama-3.2-1B, Qwen2.5-0.5B, Qwen2.5-1.5B, Gemma-2-2B and SmolLM2-1.7B. Raw delivery spans 51× (0.0003–0.0154 nats), but that figure is the min and max of *different capability kinds with different gaps*, and the smaller end is a search that nearly failed — a Phase D auditor was right to call it a misleading outlier statistic. **The honest number is the recovery fraction: 1.4%–33%, a spread of ~4×**, and 4 of 10 capability/model cells found no admissible control at all. Both readings support the gate's purpose — a ceiling measured on one base model is not a fact about another, so the per-substrate ledger stands — and both say the ceiling is lower than the earlier single-family table implied (`results/capacity_across_families.json`) |
| **D5** | Write allocation replicates | ≥10 unit pairs; fraction achieving exact preservation reported | **CLOSED 2026-08-02** — `scripts/write_allocation_replication.py`, 12 pairs across vocabulary 16/32/64 × later-unit gain 0.5/1.0/4.0, plus the compiled induction circuit as the protected unit. Orthogonal allocation preserved the first unit in **12/12** (worst error 3.05e-05, float32 noise); the shared convention preserved **0/12** and missed by up to 6.41. The control matters as much as the result: orthogonal silenced the second unit in **0** pairs, while exclusive preserved perfectly and silenced it in **12/12** — between two units with no firing condition, exclusive allocation is a veto, which is why the build path pairs it with a `when:` predicate (`results/write_allocation_replication.json`) |
| **D6** | The docs say what the code does | Per-substrate description matches `build_open`/`exec_scratch`; research components marked as such | **CLOSED 2026-08-02** — `LOOM.md` splits the claim by substrate instead of making one claim for both: "From scratch" says no downloaded weights are involved, "Open weight" says in as many words that it is *"fine-tuning and inference-time steering, and calling it anything else would be untrue"*. `VALIDATION.md` records the refusal removal with its TRIZ derivation, the two wrong policy estimators, and the limits of the word-overlap gate. `exec_scratch` reads its compute target from the device rather than hard-coding GB10 |
| **E** | The created model is worth using | A from-scratch model with held-out perplexity in the 20s–30s that answers coherently in `/use` | **RE-OPENED 2026-08-03, on its own evidence.** Three runs. (1) Perplexity 153,476 — the corpus was capped at 2MB of 250MB, so 40,000 steps made 365 passes over 1,755 sequences. (2) Perplexity **25.91**, closed on that basis — then the Phase D audit showed the held-out tenth was the *tail* of a file concatenating Simple Wikipedia, Gutenberg and CHILDES, so what was held out was child-directed transcripts. Shorter, simpler text: the number was flattering, not merely mislabelled. (3) With the split striding through the whole corpus, the honest figure is **35.18** — outside the stated band. Held-out loss 3.560 against a final training loss of 3.361, a gap of 0.20 nats, with the curve still falling: underfit, not incapable. The bar stands and the run is being extended to 90,000 steps rather than the bar being moved. Continuations are fluent and it answers through the public app (D2) |

| **F** | The side-effect guard's verdict is about the lever, not the probe set | The quantity the budget is compared against is stable across probe subsets by a clear margin | **OPEN — measured, and the failure is not the one it looked like.** `scripts/guard_resolution.py`. The raw reading moved by 0.090 across leave-one-out subsets of four probes; sixteen probes in six registers cut that to 0.029. But the guard compares `base_variety − var` with both terms on the *same* probes, so the raw level was never the error bar that mattered. Measured properly — variety lost to a deliberate perturbation — the spread is **0.042 against a 0.05 budget**, and the cause is not noise: on Llama the loss reads 0.036–0.053 across fifteen subsets and **0.011 across the sixteenth**, because one probe carries most of the damage. The verdict flips across the budget depending on whether that probe was drawn. **A mean is the wrong statistic for a guard against concentrated harm.** Builds now record `variety_lost_worst_probe` beside the mean; the threshold is deliberately unchanged, because re-judging every build already measured on an unvalidated new statistic is the mistake this file exists to prevent. Also: the reading is not portable across generation lengths (0.667 at 48 tokens, 0.958 at 12 — a shorter generation has less room to repeat), so it must never be quoted as a property of a model |

## Standing rules

1. **No number without a run.** A claim that cannot name the artifact it came from is
   deleted, not softened.
2. **Two clauses may not share one measurement.** Identical values for different
   declarations mean the instrument cannot tell them apart; say so in the report.
3. **The probe must resemble the traffic.** Every measurement failure found in this
   project so far — over-refusal invisible to its own guard, a gate that met its
   target while the model misbehaved, a demo memorised to loss 0.0002 and recalled
   never — was a probe drawn from a different distribution than the one the model
   meets.
4. **An adversarial review follows every phase**, run by an agent with no stake in
   the work, and its findings are folded in before the next phase begins.
