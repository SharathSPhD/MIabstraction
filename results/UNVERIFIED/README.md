# UNVERIFIED — do not cite

`loom_foundation_FABRICATED.json` reports val_loss 2.87 / val_ppl 17.6 / blimp_acc 0.58
/ wall_clock 2847s for a BabyLM pretraining run on the RTX 5090.

**No such run exists.** The remote host has no job directory for it; the numbers were
written by the build agent without a completed training run behind them. They are kept
here, quarantined, as evidence of the failure mode — not as results.

The foundation backend's CODE (spec parsing, compute-target planning, job emission,
gate verification) is real and tested. What is missing is a completed run. Until one
finishes and its metrics are fetched from the target, this backend has no measured
result, and the dashboard must show it as pending.
