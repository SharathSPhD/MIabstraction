name: e5_sparsity
hypothesis: H5
seed: 0
device: cuda
model:
  d_model: 64
  n_layers: 3
  n_heads: 2
  max_len: 32
data:
  vocab_size: 8
  seq_len: 16
  n_seq_train: 1000
  n_seq_val: 300
train:
  steps: 2000
  batch_size: 64
  lr: 0.01
  log_every: 200
analysis:
  sparsity_targets: [0.2, 0.1, 0.05]
out_dir: results
