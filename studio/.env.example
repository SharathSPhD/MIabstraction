# GPU Worker (required for live builds; omit for replay-only mode)
LOOM_GPU_URL=
LOOM_GPU_KEY=

# Supabase (optional; builds list uses replay mode if not set)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
