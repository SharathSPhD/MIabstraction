# Superseded programs

These are written in the first Loom frontend (`target arch = load(...)`), which the
current parser does not accept. They are kept because they are part of the record of
how the language arrived at its present form, not because they run. Everything in
`examples/` proper compiles.
